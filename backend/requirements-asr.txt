# Optional dependencies for running local ASR with Hugging Face openai/whisper-tiny.
# Install these on the robot/laptop that will run the ROS transcriber node or
# backend-side SpeechToText utility.
torch>=2.3.0
transformers>=4.45.0
accelerate>=0.33.0
numpy>=1.24.0
huggingface_hub>=0.24.0
